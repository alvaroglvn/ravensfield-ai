import { styled, XStack } from "tamagui";

export const FooterBar = styled(XStack, {
  elevation: "$1",
  background: "$background",
  paddingBlock: 25,
  paddingInline: 25,
  justify: "space-between",
  items: "center",
});
